import { COLORS } from "@/assets/colors";
import { Container, Grid } from "@mui/material";

// TODO: créer l'ui pour la landing page not logged
const Landing = () => {
  return (
    <>
      <Container>
        <Grid sx={{ backgroundColor: "black", color: COLORS.white }}>
          {" "}
          Work in progress
        </Grid>
        <Grid sx={{ backgroundColor: COLORS.backgroundButton }}>
          {" "}
          Work in progress
        </Grid>
      </Container>
    </>
  );
};

export default Landing;
